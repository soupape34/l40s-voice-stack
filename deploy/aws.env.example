# Copier vers deploy/aws.env et ajuster si besoin.
# deploy/aws.env est ignoré par git.

AWS_REGION=eu-central-1
EC2_INSTANCE_ID=i-0e278c6ee4963512e
EC2_NAME_TAG=voice-stack-test

SSH_USER=ubuntu
SSH_KEY=~/.ssh/id_ed25519
REMOTE_DIR=/home/ubuntu/l40s-voice-stack

WEB_PORT=8080
LOCAL_PORT=8080

# Rempli automatiquement par deploy/start.sh (IP publique change à chaque start)
# EC2_PUBLIC_IP=
